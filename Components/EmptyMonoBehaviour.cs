using UnityEngine;

namespace PixelInternalAPI.Components
{
	/// <summary>
	/// As the name suggest, an EmptyMonoBehaviour. It is useful for coroutines.
	/// </summary>
	public class EmptyMonoBehaviour : MonoBehaviour // Literally an empty mono behaviour with sole purpose of giving coroutines
	{
	}
}
